export type Avatar = {
    id: string;
    imageUrl: string;
    name: string;
}

export type Space = {
    id: string;
    name: string;
    thumbnail: string;
    dimensions: string;
}

export type Map = {
    id: string;
    name: string;
    thumbnail: string;
    dimensions: string;
}
